# LunaNet Gateway 1 spreading code runtime configuration
# Paths are resolved relative to this config file unless absolute.

spec_tables_dir=../docs/spec_tables
annex3_dir=../PRN/annex3/txt

annex3_gold=006_GoldCode2046hex210prns.txt
annex3_weil_primary=007_l1cp_hex210prns.txt
annex3_weil_tertiary=008_Weil1500hex210prns.txt

# Default secondary code selection (S0-S3).
# Per-PRN overrides can be supplied through docs/spec_tables/table_11_code_assignments.csv.
afs_q_secondary_code_id=S0

# Cap AFS-Q output to avoid very large allocations in baseline workflows (0 = full sequence).
afs_q_max_chips=40920
