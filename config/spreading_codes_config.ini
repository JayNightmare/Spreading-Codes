# LunaNet Gateway 1 spreading code runtime configuration
# Paths are resolved relative to this config file unless absolute.

spec_tables_dir=docs/spec_tables
annex3_dir=docs/annex3

annex3_gold=GoldCode2046hex210prns.txt
annex3_weil_primary=l1cp_hex210prns.txt
annex3_weil_tertiary=Weil1500hex210prns.txt

# Secondary code selection (S0-S3) for tiered AFS-Q generation.
afs_q_secondary_code_id=S0

# Cap AFS-Q output to avoid huge allocations (0 = full length).
afs_q_max_chips=40920
